# FAOA-2015-PREFACE bilingual and mathematical review

Date: 2026-08-24  
State: **all actionable findings applied; preface ready for locked QA**

## Review closure

The complete authority `source/upstream/preface.tex` was compared linearly with
both translation fragments and the assembled `source/id-ID/preface-id.tex`.
The review covered every narrative paragraph, all 50 reference-table rows, all
20 number-notation rows, both enumerations, the function definitions, five
citations, 53 active index hooks, source label `C0009`, and both diagrams.

The following actionable findings were applied before this record was closed:

- strengthened the Halmos paraphrase so the necessity of active practice is
  retained without reproducing the third-party quotation;
- restored the logical attachment of the sentence about finding digestible
  background material;
- added the required complex-field scope to both finite-dimensional unitary
  spectral claims;
- replaced chapter-environment residue `korolari` with the edition term
  `akibat` and repaired several stiff or incomplete Indonesian constructions;
- preserved the preface's self-description as `catatan`, not `buku`;
- preserved `peta` in the source's synonym convention and kept `onto` as the
  terminology plan's explicit recognition synonym;
- used `pracitra` as the preferred term while retaining `citra invers` as a
  recognition form;
- distinguished verbal `berkomutasi` from the noun phrase `diagram
  komutatif`;
- retained the Greek table's English names and approximate English
  pronunciation strings exactly instead of presenting localized guesses as
  source data;
- restored the two-level Fraktur table header and all 26 ordered glyph rows;
- confirmed that the split `G` in the first function condition is inside one
  dollar-delimited math span in both source and target; separately repaired
  the bare text-mode `G` in the second condition to the mathematical variable
  `$G$` and recorded that post-review repair as `PREFACE-C014`.

## Independent preservation result

- Citation order is unchanged: `Halmos:1982`, `BrownDF:1973`, `Erdman:2010`,
  `Erdman:2005`, `Erdman:2007`.
- The sole label remains `C0009`.
- All 24 Greek rows and all 26 Fraktur rows are present in source order.
- Every displayed number/function formula and both diagram topologies are
  preserved. The only mathematical-semantic changes are the explicitly
  recorded complex-field clarification and the three nonnegative-set glosses.
- Both `tabularx` groups close correctly; no excluded `TABLE.TEX` macro enters
  the target.
- All fourteen ledger markers occur once; the thirteen pretranslation records
  retain their stable IDs and the post-review TeX repair is appended as
  `FAOA-2015-PREFACE-CORR-014`.

The final target identity and applied ledger identity are bound by the locked
translation checker and admission receipt rather than duplicated here.
